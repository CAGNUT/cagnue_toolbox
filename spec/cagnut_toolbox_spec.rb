require 'spec_helper'

describe CagnutToolbox do
  it 'has a version number' do
    expect(CagnutToolbox::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
